import { describe, test } from "vitest";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import App from "../App";

describe("반복 일정 기능 테스트", () => {
  describe("1. 반복 유형 선택", () => {
    test.fails("일정 생성 시 반복 유형을 선택할 수 있다", () => {});

    test.fails("일정 수정 시 반복 유형을 선택할 수 있다", () => {});

    test.fails(
      "모든 반복 유형(매일, 매주, 매월, 매년)을 선택할 수 있다",
      () => {}
    );
  });

  describe("2. 반복 간격 설정", () => {
    test.fails("매일 반복에 대해 간격을 설정할 수 있다", () => {});

    test.fails("매주 반복에 대해 간격을 설정할 수 있다", () => {});

    test.fails("매월 반복에 대해 간격을 설정할 수 있다", () => {});

    test.fails("매년 반복에 대해 간격을 설정할 수 있다", () => {});
  });

  describe("3. 반복 일정 표시", () => {
    test.fails(
      "주간 뷰에서 반복 일정이 시각적으로 구분되어 표시된다",
      () => {}
    );

    test.fails(
      "월간 뷰에서 반복 일정이 시각적으로 구분되어 표시된다",
      () => {}
    );
  });

  describe("4. 예외 날짜 처리", () => {
    test("반복 일정 중 특정 날짜를 제외할 수 있다", () => {});

    test("반복 일정 중 특정 날짜의 일정을 수정할 수 있다", () => {});
  });

  describe("5. 반복 종료 조건", () => {
    test("특정 날짜까지 반복되도록 설정할 수 있다", () => {});

    test("특정 횟수만큼 반복되도록 설정할 수 있다", () => {});

    test("종료 없이 계속 반복되도록 설정할 수 있다", () => {});
  });

  describe("6. 요일 지정 (주간 반복의 경우)", () => {
    test("주간 반복 시 특정 요일을 선택할 수 있다", () => {});
  });

  describe("7. 월간 반복 옵션", () => {
    test("매월 특정 날짜에 반복되도록 설정할 수 있다", () => {});

    test("매월 특정 순서의 요일에 반복되도록 설정할 수 있다", () => {});
  });

  describe("8. 반복 일정 수정", () => {
    test("반복 일정의 단일 일정을 수정할 수 있다", () => {});

    test("반복 일정의 모든 일정을 수정할 수 있다", () => {});
  });
});
